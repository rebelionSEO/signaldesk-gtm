import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const studies = sqliteTable('studies', { id: text('id').primaryKey(), owner: text('owner').notNull(), planner:text('planner'), operations:text('operations').notNull().default('{"metrics":[],"tasks":[],"outcomes":[]}'), trace:text('trace').notNull().default('[]'), brief: text('brief').notNull(), report: text('report').notNull(), research: text('research'), candidate: text('candidate'), stage: text('stage').notNull().default('draft'), revision: integer('revision').notNull().default(0), lease: text('lease'), busyUntil: integer('busy_until').notNull().default(0), createdAt: text('created_at').notNull(), updatedAt: text('updated_at').notNull() }, t => [index('studies_owner_updated').on(t.owner, t.updatedAt)]);
export const runCalls = sqliteTable('run_calls', { id: text('id').primaryKey(), owner: text('owner').notNull(), createdAt: integer('created_at').notNull() }, t => [index('calls_owner_created').on(t.owner, t.createdAt)]);
